name: association_appli
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dartz: ^0.10.1
  get_it: ^8.2.0
  provider: ^6.1.5+1
  path: ^1.9.1
  sqflite: ^2.4.2
  bottom_bar_matu: ^1.5.0
  introduction_screen: ^4.0.0
  smooth_page_indicator: ^1.2.1
  google_fonts: ^6.3.2
  flutter_slidable: ^4.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
    - assets/images/
